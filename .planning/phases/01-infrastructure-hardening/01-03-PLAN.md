---
phase: 01-infrastructure-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - index.html
autonomous: true
requirements:
  - INFRA-03
  - INFRA-08

must_haves:
  truths:
    - "No render function crashes with TypeError when its required D key is missing — all 18 render functions return early if data isn't ready"
    - "No raw API string (player name, team name, manager name) is injected into innerHTML without esc() wrapping — XSS surface is closed"
  artifacts:
    - path: "index.html"
      provides: "Guard clauses at top of all render functions + esc() wrapping at all API string injection sites"
  key_links:
    - from: "D.teams (populated by buildCurrentSeasonData)"
      to: "renderPower(), renderRosters(), renderContracts(), renderGM() and others"
      via: "if (!D.teams) return; guard at function top"
      pattern: "function render\\w+\\(\\)[\\s\\S]{0,50}if.*!D\\."
    - from: "esc() at line 1408"
      to: "every p.name / p.nm / t.name / owner_name injection site"
      via: "esc() wrapping at template literal injection point"
      pattern: "esc\\(p\\.name\\|p\\.nm\\|t\\.name"
---

<objective>
One pass through all 18 render functions that accomplishes two things simultaneously: add guard clauses at the top of every function that directly accesses D keys, and wrap every raw API string interpolation in esc().

Purpose: Batching these in one plan avoids touching the same 18 functions twice. Guard clauses prevent silent crashes during progressive data loading. esc() coverage closes the XSS surface of 109+ unescaped API string injections.
Output: Updated index.html with guard clauses in all render functions and esc() at every API string injection site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-hardening/01-CONTEXT.md
@.planning/phases/01-infrastructure-hardening/01-RESEARCH.md
</context>

<interfaces>
<!-- All 18 render function locations and their required D keys. Verified by direct inspection. -->

| Render Function | Line | First D Access | Guard Needed? |
|-----------------|------|----------------|---------------|
| renderTab() | 2634 | dispatches to others | No direct D access |
| renderPower() | 2824 | D.teams.map(...) | YES — D.teams |
| renderRosters() | 2994 | D.teams.reduce(...) | YES — D.teams |
| renderTrophies() | 3042 | D.champions | YES — D.champions |
| renderTrades() | 3248 | D.unified_trades\|\|[] | Soft guard already present (||[] fallback) |
| renderMatchups() | 3697 | D.matchup_weeks | YES — D.matchup_weeks |
| renderDraft() | 3919 | Inspect at runtime — read first | YES if direct D access |
| renderAge() | 4086 | Inspect at runtime — read first | YES if direct D access |
| renderScoring() | 4165 | Inspect at runtime — read first | YES if direct D access |
| renderAwards() | 4252 | Inspect at runtime — read first | YES if direct D access |
| renderAnalytics() | 4318 | Inspect at runtime — read first | YES if direct D access |
| renderMoves() | 4464 | Inspect at runtime — read first | YES if direct D access |
| renderGM() | 4522 | Inspect at runtime — read first | YES if direct D access |
| renderRivals() | 4751 | Inspect at runtime — read first | YES if direct D access |
| renderContracts() | 4780 | if(!D.contracts) already present | ALREADY GUARDED — verify pattern |
| renderConstitution() | 5004 | Inspect at runtime — read first | YES if direct D access |
| renderWarRoom() | 5036 | Inspect at runtime — read first | YES if direct D access |
| renderPulse() | 5140 | Inspect at runtime — read first | YES if direct D access |
| renderChronicle() | 5226 | Inspect at runtime — read first | YES if direct D access |

Guard pattern (per D-03, D-04):
```javascript
function renderPower() {
  if (!D.teams) return;  // Guard: data not yet populated — dirty-flag will re-render
  // ... rest unchanged ...
}
```
ONE line. No spinner. No error message. Just return.

esc() function location (line 1408):
```javascript
const esc=s=>typeof s==='string'?s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;'):s;
```

CRITICAL — Double-escape risk (from research Pitfall 1):
- p.fn (full_name field) — already esc()'d when playerDB is built at line 1577. Do NOT wrap p.fn again.
- p.name — verify where p.name is assigned in buildCurrentSeasonData(). If it's assigned from p.fn, it's already escaped. If assigned from raw API, wrap it.
- Check the assignment site before blindly wrapping p.name.

Fields that ARE raw API strings (safe to wrap):
- t.name (team name from Sleeper — raw)
- owner_name (manager name — raw at assignment)
- p.team (NFL team abbreviation — raw)

Fields to verify before wrapping:
- p.name — trace assignment in buildCurrentSeasonData()
- p.nm — trace assignment
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit and add guard clauses to all render functions</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 2824-2828 (renderPower — add guard for D.teams)
    - index.html lines 2994-2998 (renderRosters — add guard for D.teams)
    - index.html lines 3042-3048 (renderTrophies — check first D access)
    - index.html lines 3697-3703 (renderMatchups — add guard for D.matchup_weeks)
    - index.html lines 3919-3930 (renderDraft — check first D access)
    - index.html lines 4086-4095 (renderAge — check first D access)
    - index.html lines 4165-4175 (renderScoring — check first D access)
    - index.html lines 4252-4262 (renderAwards — check first D access)
    - index.html lines 4318-4330 (renderAnalytics — check first D access)
    - index.html lines 4464-4475 (renderMoves — check first D access)
    - index.html lines 4522-4535 (renderGM — check first D access)
    - index.html lines 4751-4762 (renderRivals — check first D access)
    - index.html lines 4780-4785 (renderContracts — already guarded, verify and leave)
    - index.html lines 5004-5015 (renderConstitution — check first D access)
    - index.html lines 5036-5048 (renderWarRoom — check first D access)
    - index.html lines 5140-5152 (renderPulse — check first D access)
    - index.html lines 5226-5238 (renderChronicle — check first D access)
  </read_first>
  <action>
    Read each render function listed above. For every function that directly accesses a D key without first checking it exists, add a one-line guard at the very top of the function body (line 1 inside the function, before any other code).

    Guard format — use the key(s) that would crash if missing:
    ```javascript
    function renderPower() {
      if (!D.teams) return;
      // existing code unchanged from here
    ```

    For functions that access multiple D keys (e.g., D.teams AND D.matchup_weeks), guard on the one accessed first. If both are accessed immediately, use:
    ```javascript
    if (!D.teams || !D.matchup_weeks) return;
    ```

    renderContracts() already has a guard that shows an error message — leave it exactly as-is. Its pattern is intentionally different (contracts tab explicitly communicates empty state).

    renderTrades() uses `D.unified_trades||[]` — this is a soft guard. Leave it — it already handles the missing-data case.

    Do NOT add guards to renderTab() — it is a dispatcher, not a data renderer.

    After adding guards, run a mental self-check: can any render function still throw TypeError: Cannot read properties of undefined after this change? If yes, add the missing guard.
  </action>
  <verify>
    <automated>grep -A2 'function renderPower' /Users/aaronwoods/harambes-dozen-repo/index.html | grep 'if.*!D\.'</automated>
    Should return the guard line.
    <automated>grep -A2 'function renderRosters' /Users/aaronwoods/harambes-dozen-repo/index.html | grep 'if.*!D\.'</automated>
    Should return the guard line.
    <automated>grep -A2 'function renderMatchups' /Users/aaronwoods/harambes-dozen-repo/index.html | grep 'if.*!D\.'</automated>
    Should return the guard line.
  </verify>
  <done>
    Every render function that directly accesses a D key has a guard clause as its first statement. No render function can throw TypeError on a missing D key. renderContracts() guard is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply esc() to all API string injection sites across all render functions</name>
  <files>index.html</files>
  <read_first>
    - index.html lines 1570-1600 (playerDB construction — check if p.fn is esc()'d here, and where p.name is assigned)
    - index.html lines 1780-1810 (buildCurrentSeasonData — check where owner_name, team_name are set)
    - index.html lines 2824-2995 (renderPower — find all p.name, t.name injections)
    - index.html lines 2994-3042 (renderRosters — find all p.name, t.name injections)
    - index.html lines 3042-3248 (renderTrophies — find all name injections)
    - index.html lines 3248-3697 (renderTrades — find all name injections)
    - index.html lines 3697-3919 (renderMatchups — find all name injections)
    - index.html lines 3919-4780 (remaining render functions — find all name injections)
    - index.html lines 5500-5570 (openPP modal — already has some esc() calls, verify completeness)
  </read_first>
  <action>
    **Step 1 — Trace p.name and p.nm assignment (read before editing):**
    Search for where `p.name` and `p.nm` are assigned in the data pipeline (buildCurrentSeasonData, around lines 1780-1850). Determine:
    - Is `p.name` sourced from `p.fn` (already esc()'d at line 1577) or from raw API data?
    - Is `p.nm` the same as `p.name` or a separate raw field?

    If `p.name = p.fn` (already escaped), do NOT wrap `p.name` in esc() — this would produce `&amp;` appearing as literal text in the UI.
    If `p.name` is sourced from raw Sleeper API player object directly (not from the escaped `p.fn`), then wrap it.

    **Step 2 — Apply esc() at every HTML injection site:**
    For ALL of these patterns in render functions and openPP(), wrap in esc():
    - `t.name` → `esc(t.name)` (team name — raw Sleeper API string)
    - `t.team_name` → `esc(t.team_name)` (same)
    - `owner_name` → `esc(owner_name)` (manager display name — raw)
    - Any other user-display-name field from Sleeper API

    Apply esc() ONLY at the HTML injection point (inside template literals and string concatenation that produces innerHTML). Do NOT apply at data assignment points — that creates the double-escape problem.

    Pattern for string concatenation:
    ```javascript
    // BEFORE:
    h += '<div>' + p.name + '</div>';
    // AFTER:
    h += '<div>' + esc(p.name) + '</div>';
    ```

    Pattern for template literals:
    ```javascript
    // BEFORE:
    h += `<div>${p.name}</div>`;
    // AFTER:
    h += `<div>${esc(p.name)}</div>`;
    ```

    **Step 3 — Verify openPP() modal:**
    Lines ~5500-5570 already have some esc() calls. Read these lines and confirm every name/team field is wrapped. Add any missing wrappings.

    **Do NOT escape:**
    - Numeric values (points, rankings, ages) — these cannot contain HTML injection
    - Already-escaped fields identified in Step 1
    - Static strings (labels, UI text not from API)
    - HTML class names or IDs you construct (these come from your own code, not API)
  </action>
  <verify>
    <automated>grep -n "'\+p\.name\|'\+p\.nm\|'\+t\.name\|'\+owner_name\|'\+team_name" /Users/aaronwoods/harambes-dozen-repo/index.html | grep -v 'esc(' | head -20</automated>
    Should return 0 results (all string concatenation sites are wrapped).
    <automated>grep -n '`\${p\.name}\|`\${p\.nm}\|`\${t\.name}\|`\${owner_name}' /Users/aaronwoods/harambes-dozen-repo/index.html | grep -v 'esc(' | head -20</automated>
    Should return 0 results (all template literal sites are wrapped).
    <automated>grep -c 'esc(' /Users/aaronwoods/harambes-dozen-repo/index.html</automated>
    Should return significantly more than 6 (the prior count) — expect 40+ if coverage is comprehensive.
  </verify>
  <done>
    - All t.name, team_name, owner_name, and any other user-supplied string fields are wrapped in esc() at every HTML injection site
    - Fields confirmed already-escaped at data pipeline time are not double-wrapped
    - openPP() modal has complete esc() coverage
    - esc() call count is substantially higher than the baseline of 6
  </done>
</task>

</tasks>

<verification>
Grep checks:
- `grep -A2 'function renderPower' index.html | grep 'if.*!D\.'` → returns guard line
- `grep -A2 'function renderMatchups' index.html | grep 'if.*!D\.'` → returns guard line
- `grep -A2 'function renderRosters' index.html | grep 'if.*!D\.'` → returns guard line
- `grep -n "'\+t\.name" index.html | grep -v 'esc('` → 0 results
- `grep -n "'\+owner_name" index.html | grep -v 'esc('` → 0 results
- `grep -c 'esc(' index.html` → 40+ (up from baseline of 6)
</verification>

<success_criteria>
- All 17 data-rendering functions (excludes renderTab dispatcher) have guard clauses — no TypeError on missing D keys
- All team names, manager names, and other API-sourced strings wrapped in esc() at injection sites — XSS surface closed
- No double-escaping of fields already escaped at pipeline time
- renderContracts() guard unchanged (it intentionally shows an empty-state message)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-hardening/01-03-SUMMARY.md`
</output>
