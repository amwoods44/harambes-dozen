---
phase: 03.2-broadcast-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 03.2-01
  - 03.2-02
files_modified:
  - index.html
autonomous: true
requirements:
  - POLI-01
  - POLI-05

must_haves:
  truths:
    - "Hero LEAGUE LORE card is wider and commands more visual weight"
    - "Countdown clock shows 'UNTIL' prefix before 'NFL KICKOFF'"
    - "Stat strip items feel like ESPN ticker — contrast, elevation, separator lines"
    - "Cast strip has a subtle separator after every 4th team on desktop"
    - "Draft tab shows 'Rookie draft not yet completed for 2026' with Harambe watermark instead of 'No data'"
    - "Hero feature text wraps cleanly on mobile 375px"
    - "Cast strip on mobile shows scroll indicator dots or gradient fade"
    - "Contracts team filter pills use horizontal scroll on mobile instead of 4-row wrap"
    - "Keeper sheet has overflow-x:auto with horizontal scroll on mobile"
    - "Power score circle shows 64px size on first render"
  artifacts:
    - path: "index.html"
      provides: "Updated hero layout, countdown clock HTML, improved stat strip, draft empty state, mobile responsive fixes"
      contains: "UNTIL"
  key_links:
    - from: "hero HTML section"
      to: "hero-hist element"
      via: "buildHeroHist() / static HTML"
      pattern: "hero-hist"
    - from: "renderDraft() empty state"
      to: "draft-empty CSS class"
      via: "innerHTML + .draft-empty"
      pattern: "draft-empty"
---

<objective>
Polish the hero section, countdown clock, stat strip, cast strip, draft empty state, and mobile layout issues. These require targeted HTML and JS changes in specific render functions and static HTML.

Purpose: The hero is the first 3-second impression. The draft empty state prevents a confusing "No data" screen during offseason. Mobile fixes make the app feel designed for phones, not shrunk.

Output: Updated static hero HTML (D-21, D-22, D-23, D-24), draft empty state in renderDraft() (D-27), mobile layout fixes for hero text, cast strip, stat strip, Contracts filter pills, and keeper table (D-28 through D-33), Power score circle size increase (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.2-broadcast-polish/03.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero section + countdown + stat strip + cast strip</name>
  <files>index.html</files>
  <read_first>
    Find and read these sections of index.html:
    1. The hero HTML (search for `class="hero"` in the body — around line 1800-1850). Read the full hero block including `.hero-ey`, `.hero-h`, `.hero-tag`, `.hero-bar`, `.hero-hist` elements.
    2. The countdown clock HTML (search for `nfl-cd` class in the HTML body — around line 1780 in the header). Read the `class="nfl-cd"` element fully.
    3. The stat strip HTML (search for `class="sstrip"` — around line 1860). Read all `.ss` elements.
    4. The cast strip HTML (search for `class="cast"` — around line 1870). Note the `.cast-in` and `.cast-fig` elements.
    5. The `.hero-tag` CSS rule (around line 180) for the feature text.
    6. The `@media(max-width:600px)` rule for `.sstrip` (around line 704-705) — the `display:none` on nth-child 4+.
    Also check: the `updateCD()` function in the JS (search for `updateCD` to find where the countdown is populated — the JS populates the `.nfl-cd-num` elements). Note that you will only change the HTML label, not the JS logic.
  </read_first>
  <action>
    **Change 1: Hero LEAGUE LORE card width (D-21)**
    Find `.hero-hist` CSS rule (around line 200):
    ```css
    .hero-hist{padding:16px 20px 0;text-align:center;min-height:32px}
    ```
    Add to the CSS section (in the Phase 03.2 additions area) — do NOT change the original rule, ADD an override:
    ```css
    /* D-21: Hero LEAGUE LORE card — wider, more commanding */
    .hero-hist{padding:20px 24px 0}
    .hero-hist-vintage{max-width:600px;width:100%;margin:0 auto}
    .hero-hist-inner{max-width:500px;padding:8px 20px}
    ```

    **Change 2: Countdown clock — add "UNTIL" label (D-22)**
    Find the countdown clock HTML in the header (search for `class="nfl-cd"` in the body HTML). It contains `.nfl-cd-units` and `.nfl-cd-tag`. Find the element that shows "NFL KICKOFF" text. Add "UNTIL" as a prefix.

    Look for the HTML structure like:
    ```html
    <div class="nfl-cd-tag"><span>NFL KICKOFF</span></div>
    ```
    Change it to:
    ```html
    <div class="nfl-cd-tag">UNTIL<span>NFL KICKOFF</span></div>
    ```
    (or whatever the exact HTML structure is — find it first, then add "UNTIL" as prefix text in the `.nfl-cd-tag` element)

    **Change 3: Stat strip ESPN-ticker feel (D-23)**
    Add CSS for stat strip elevation:
    ```css
    /* D-23: Stat strip ESPN ticker treatment */
    .sstrip{border-top:2px solid var(--g5);border-bottom:2px solid var(--g5);box-shadow:0 2px 12px rgba(0,0,0,.4),inset 0 1px 0 rgba(255,255,255,.03)}
    .ss{border-right:1px solid var(--g5)}
    .ss:last-child{border-right:none}
    ```

    **Change 4: Cast strip separator every 4th team (D-24)**
    Find the cast strip HTML. It has 12 `.cast-fig` elements. Add `data-sep="true"` on the 4th, 8th elements:
    - Find the 4th `.cast-fig` in the HTML and add class `cast-sep` to it
    - Find the 8th `.cast-fig` and add class `cast-sep` to it
    Then add CSS:
    ```css
    /* D-24: Cast strip separator after 4th and 8th team */
    .cast-fig.cast-sep{margin-right:16px}
    .cast-fig.cast-sep::before{content:'';position:absolute;right:-10px;top:10%;height:80%;width:1px;background:rgba(255,255,255,.08);pointer-events:none}
    ```
    NOTE: `.cast-fig` has `position:relative` already (confirmed in read). The `::before` on `.cast-fig.cast-sep` will be a visible separator line between team groups.

    **Change 5: Hero feature text mobile fix (D-28)**
    Find the `.hero-tag` HTML in the body. It likely contains "15 KEEPERS * PICK TRADING * BONUS SCORING".
    The CSS fix:
    ```css
    /* D-28: Hero tag mobile wrapping fix */
    @media(max-width:480px){
      .hero-tag{font-size:13px;letter-spacing:2px}
      .hero-tag b{display:inline}
    }
    ```

    **Change 6: Stat strip mobile scroll (D-30)**
    Override the current `display:none` behavior on mobile to instead use horizontal scroll:
    ```css
    /* D-30: Stat strip mobile — horizontal scroll instead of truncate */
    @media(max-width:600px){
      .sstrip{overflow-x:auto;flex-wrap:nowrap;-webkit-overflow-scrolling:touch}
      .sstrip .ss:nth-child(n+4){display:flex}
      .ss{min-width:80px;flex-shrink:0}
    }
    ```
    NOTE: This overrides the existing `@media(max-width:768px){ .sstrip .ss:nth-child(n+4){display:none} }` rule. Place the override AFTER the existing rule so it takes precedence.

    **Change 7: Cast strip mobile scroll indicator (D-29)**
    Add CSS for a subtle indicator:
    ```css
    /* D-29: Cast strip mobile scroll indication */
    @media(max-width:600px){
      .cast::after{width:80px}
    }
    ```
    (The existing `::after` gradient already exists — just make it wider on mobile for stronger indication)
  </action>
  <verify>
    <automated>grep -n "UNTIL" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "UNTIL" index.html` returns the countdown clock HTML with "UNTIL" text
    - `grep "hero-hist-vintage.*max-width:600px" index.html` OR `grep "max-width:600px" index.html` returns the hero card override
    - `grep "cast-sep" index.html` returns both the CSS rule and 2 HTML instances in the cast strip
    - `grep "D-28.*Hero tag" index.html` OR `grep "hero-tag.*font-size:13px" index.html` returns the mobile fix
    - `grep "min-width:80px.*flex-shrink:0" index.html` returns the stat strip mobile scroll rule
    - The stat strip still shows on desktop (grep `sstrip .ss:nth-child(n+4){display:flex}` under mobile breakpoint only)
  </acceptance_criteria>
  <done>Hero card wider; countdown shows UNTIL; stat strip has elevation; cast strip has team separators; mobile hero/stat strip/cast strip fixes applied</done>
</task>

<task type="auto">
  <name>Task 2: Draft empty state + Contracts mobile + Keeper mobile + Power circle size</name>
  <files>index.html</files>
  <read_first>
    Find and read:
    1. The `renderDraft()` function — search for `function renderDraft(` in the JS. Read the section that handles empty/no-draft state (look for "No draft" or "no picks" conditional). This is where the empty state HTML is injected.
    2. The `renderContracts()` function — search for `function renderContracts(` or `renderContracts`. Find the team filter pills section (search for "team filter" or "con-filter" nearby). Read the HTML for the filter pills container.
    3. The keeper sheet section in renderContracts — search for `keeper-tbl` or `renderKeeperTable` to find where the table is rendered.
    4. The Power ranking card rendering — search for `.prk` usage in the JS to find where the power score circle HTML is built. Read the `prk-n` value.
    5. The CSS for `.prk` (around line 259) — current 52px width/height.
    Also read the `D.isOffseason` condition in renderDraft() from Phase 03.1 plans.
  </read_first>
  <action>
    **Change 1: Draft empty state (D-27)**
    In `renderDraft()`, find the empty/no-data fallback. The existing code (from Phase 03.1 plan 03) likely has something like:
    ```javascript
    // When no draft picks exist for any year
    h = '<div class="u-empty">No draft data</div>';
    ```
    Replace the bare empty state with the broadcast-quality version:
    ```javascript
    h = `<div class="draft-empty">
      <div class="draft-empty-watermark">🦍</div>
      <div class="draft-empty-text">ROOKIE DRAFT NOT YET COMPLETED FOR ${CURRENT_YEAR}</div>
      <div class="draft-empty-sub">Check back after the rookie draft concludes</div>
    </div>`;
    ```
    NOTE: The Harambe gorilla emoji (`🦍`) in the watermark — per CLAUDE.md "No Emojis in App UI". Use the project logo text instead:
    ```javascript
    h = `<div class="draft-empty">
      <div class="draft-empty-watermark" style="font-family:var(--fd);font-size:100px;font-weight:900;color:var(--t1);opacity:.04;letter-spacing:-4px">HD</div>
      <div class="draft-empty-text">ROOKIE DRAFT NOT YET COMPLETED FOR ${CURRENT_YEAR}</div>
      <div class="draft-empty-sub">Check back after the rookie draft concludes</div>
    </div>`;
    ```
    If the empty state condition is inside an offseason guard or `if (!picks || picks.length === 0)` block — find it and replace the inner `h` assignment. Do NOT change the logic, only the HTML output.

    **Change 2: Contracts team filter pills — mobile horizontal scroll (D-31)**
    In `renderContracts()`, find the team filter HTML. It likely generates:
    ```html
    <div class="con-filters" style="display:flex;flex-wrap:wrap;gap:6px;...">
    ```
    Replace `flex-wrap:wrap` with a scrollable container. The best approach: add the CSS class `scroll-x-strip` (defined in Plan 01) to the filter container div.
    Find the HTML string that builds the filter pills container and change:
    ```javascript
    // Before:
    `<div style="display:flex;flex-wrap:wrap;gap:6px;...`
    // After:
    `<div class="con-team-filters" style="display:flex;gap:6px;overflow-x:auto;-webkit-overflow-scrolling:touch;padding:4px 0;scrollbar-width:none;...`
    ```
    Also add CSS:
    ```css
    /* D-31: Contracts team filter mobile scroll */
    .con-team-filters{display:flex;gap:6px;overflow-x:auto;flex-wrap:nowrap;-webkit-overflow-scrolling:touch;scrollbar-width:none;padding:4px 0}
    .con-team-filters::-webkit-scrollbar{display:none}
    @media(min-width:601px){.con-team-filters{flex-wrap:wrap}}
    ```

    **Change 3: Keeper sheet mobile horizontal scroll (D-33)**
    Find the keeper sheet container in `renderContracts()` or `renderKeeperTable()`. The table is `class="keeper-tbl"`. Ensure its container has `overflow-x:auto`.
    Add CSS:
    ```css
    /* D-33: Keeper sheet mobile scroll */
    .keeper-tbl-wrap{overflow-x:auto;-webkit-overflow-scrolling:touch;border-radius:8px;border:1px solid var(--g4)}
    .keeper-tbl-wrap .keeper-tbl{min-width:600px}
    ```
    In the JS, find where the `<table class="keeper-tbl">` is built and wrap it:
    ```javascript
    // Wrap the table in a scrollable container
    // Change: `<table class="keeper-tbl">...`
    // To: `<div class="keeper-tbl-wrap"><table class="keeper-tbl">...`
    // And add `</div>` after `</table>`
    ```

    **Change 4: Power score circle — 64px hero size (D-05)**
    Find the CSS for `.prk` (around line 259):
    ```css
    .prk{width:52px;height:52px;...}
    .prk-n{font-size:var(--text-xl);...}
    ```
    Add override in the Phase 03.2 CSS section:
    ```css
    /* D-05: Power score circle — 64px hero size */
    .prk{width:64px;height:64px;border-radius:10px}
    .prk-n{font-size:var(--text-2xl)}
    ```
    (var(--text-2xl) = 34px, which is the right size for the hero stat)

    **Change 5: GM Dashboard record prominence (D-08)**
    Find `renderGM()` in the JS. The manager card renders stats via `.mgr-sv` elements. Find where RECORD is rendered. Currently `font-size:var(--text-md)` (16px). Add CSS override to make the record stat larger:
    ```css
    /* D-08: GM record as first/dominant stat */
    .mgr-stat:first-child .mgr-sv{font-size:var(--text-2xl);color:var(--t1)}
    .mgr-stat:first-child .mgr-sl{font-size:var(--text-2xs);letter-spacing:2px}
    ```
    This uses CSS structural selectors to avoid JS changes — works if the RECORD stat is consistently the first in the grid.
  </action>
  <verify>
    <automated>grep -n "ROOKIE DRAFT NOT YET COMPLETED" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ROOKIE DRAFT NOT YET COMPLETED" index.html` returns the broadcast-quality empty state HTML in renderDraft()
    - `grep "con-team-filters" index.html` returns both the CSS class and the JS usage in renderContracts()
    - `grep "keeper-tbl-wrap" index.html` returns both the CSS definition and the JS wrapping in renderKeeperTable()/renderContracts()
    - `grep "prk.*width:64px" index.html` returns the CSS override
    - `grep "mgr-stat:first-child .mgr-sv" index.html` returns the GM record size override
    - No gorilla emoji in the draft empty state (grep for `🦍` should return 0 results)
  </acceptance_criteria>
  <done>Draft empty state upgraded to broadcast quality; Contracts filter pills scroll on mobile; keeper table wrapped in horizontal scroll container; power circle enlarged to 64px; GM record stat made dominant</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep "UNTIL" index.html` returns the countdown HTML change
- `grep "draft-empty-text" index.html` returns both CSS and JS usage
- `grep "keeper-tbl-wrap" index.html` returns 2+ instances (CSS + JS)
- `grep "con-team-filters" index.html` returns 2+ instances (CSS + JS)
- `grep "prk.*64px" index.html` returns the size override
- `grep "cast-sep" index.html` returns 2 HTML instances + 1 CSS rule
</verification>

<success_criteria>
Hero is more commanding. Countdown says "UNTIL NFL KICKOFF." Draft never shows bare "No data." Mobile hero text wraps cleanly. Stat strip is scrollable on mobile. Contracts filter pills scroll horizontally. Keeper table has horizontal scroll on mobile. Power circles are 64px. GM record is the dominant stat.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-broadcast-polish/03.2-03-SUMMARY.md`
</output>
