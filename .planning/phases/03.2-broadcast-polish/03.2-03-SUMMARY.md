---
phase: 03.2-broadcast-polish
plan: 03
subsystem: ui
tags: [css, vanilla-js, mobile-responsive, hero, broadcast-design]

# Dependency graph
requires:
  - phase: 03.2-01
    provides: CSS utility classes (scroll-x-strip, u-card variants), spacing normalization
  - phase: 03.2-02
    provides: Draft empty state CSS (.draft-empty), transition and hover CSS, D-27 classes

provides:
  - Hero LEAGUE LORE card wider (600px max-width, larger padding)
  - Countdown clock shows UNTIL NFL KICKOFF
  - Stat strip ESPN ticker elevation (border, box-shadow)
  - Cast strip separator lines after 4th and 8th teams
  - Draft tab broadcast-quality empty state (HD watermark + message)
  - Contracts team filter pills horizontal scroll on mobile
  - Keeper sheet table horizontal scroll on mobile
  - Power score circle enlarged to 64px
  - GM Dashboard record stat dominant (2xl font, t1 color)
  - Mobile hero-tag wrapping fix at 480px
  - Mobile cast strip wider fade indicator at 600px
  - Mobile stat strip horizontal scroll (all 5 items accessible)

affects: [03.2-04, 03.2-05, 03.2-06, future mobile polish plans]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CSS-only structural selectors for targeting first stats (.mgr-stat:first-child)"
    - "Cast strip separator via position:relative + ::before pseudo-element (D-24 pattern)"
    - "Mobile-first override pattern: desktop flex-wrap:wrap, mobile flex-wrap:nowrap + overflow-x:auto"
    - "Template literal in renderDraft() for broadcast empty state with CSS class-driven watermark"

key-files:
  created: []
  modified:
    - index.html

key-decisions:
  - "Added cast-sep class via JS (buildCast idx 3 and 7) rather than nth-child CSS — avoids fragility if team order changes"
  - "Countdown UNTIL rendered as <span class=nfl-cd-until> so it can be styled independently from NFL KICKOFF text"
  - "keeper-tbl-wrap wraps only the <table> not the surrounding .bh — keeps header visible outside the scroll zone"
  - "D-31 con-team-filters overrides entire flex container via CSS class, removing inline style — cleaner mobile override"
  - "Draft empty state uses selDraft||CURRENT_YEAR to show the selected year that has no picks, not a hardcoded value"

patterns-established:
  - "Mobile scroll pills: use class with flex-wrap:nowrap + overflow-x:auto + scrollbar-width:none, override to flex-wrap:wrap at 601px"
  - "Table mobile scroll: wrap in .keeper-tbl-wrap, set min-width on table inside — header stays sticky"

requirements-completed: [POLI-01, POLI-05]

# Metrics
duration: 4min
completed: 2026-04-01
---

# Phase 03.2 Plan 03: Hero Polish + Mobile Responsive Fixes Summary

**Hero card wider, countdown says UNTIL, stat strip elevated, draft never shows bare "No data", all mobile layout issues fixed for hero/cast/stat/contracts/keeper**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-01T19:03:26Z
- **Completed:** 2026-04-01T19:07:16Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Hero LEAGUE LORE card commands more visual weight at 600px max-width with generous padding
- Countdown clock now reads "UNTIL / NFL KICKOFF" — more dramatic, broadcast-appropriate
- Stat strip elevated with 2px borders and box-shadow for ESPN ticker feel; separator lines between items
- Cast strip gets visible group breaks after team 4 and team 8 via `cast-sep` class in `buildCast()`
- Draft empty state upgraded from bare "No draft data" to broadcast-quality "HD" watermark + year-specific message
- Contracts filter pills scroll horizontally on mobile instead of wrapping to 4+ rows
- Keeper sheet table has proper horizontal scroll container with border treatment
- Power score circles are 64px (up from 52px) — the hero number on Power tab
- GM Dashboard record stat is visually dominant via CSS structural selector
- Five mobile breakpoint fixes applied (hero tag, cast fade, stat strip scroll, 480px/600px)

## Task Commits

1. **Task 1: Hero section + countdown + stat strip + cast strip** - `8775232` (feat)
2. **Task 2: Draft empty state + Contracts mobile + Keeper mobile + Power circle size** - `2f63895` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `/Users/aaronwoods/harambes-dozen-repo/index.html` - CSS additions in Phase 03.2 Plan 03 block; updateCD() UNTIL prefix; buildCast() cast-sep on idx 3+7; renderDraft() broadcast empty state; renderContracts() con-team-filters class; renderKeeperTable() keeper-tbl-wrap

## Decisions Made
- Cast sep implemented in JS (index 3, 7) rather than CSS nth-child — more robust when teams reorder
- Countdown UNTIL in a separate `<span class="nfl-cd-until">` — independent styling, won't be confused with main label
- `con-team-filters` replaces the inline `display:flex;flex-wrap:wrap;margin-bottom:16px` entirely via CSS class — eliminates redundancy
- Draft empty state uses `selDraft||CURRENT_YEAR` so it always shows the selected draft year that has no picks

## Deviations from Plan

None — plan executed exactly as written. The `--text-2xl` token referenced in the plan exists in the CSS design tokens and is used correctly for `.prk-n` and `.mgr-stat:first-child .mgr-sv`.

## Known Stubs

None — all changes are live CSS/JS with no placeholder data.

## Issues Encountered

None — all sections found at expected line ranges. CSS cascade order confirmed: Plan 03 additions at end of `<style>` block correctly override earlier rules.

## Next Phase Readiness

- Hero section changes are visible immediately on page load (static HTML + CSS)
- All mobile fixes active at 480px and 600px breakpoints
- `.con-team-filters` and `.keeper-tbl-wrap` CSS classes defined and ready for use in any future contracts work (Plans 04-06)
- Power circle at 64px sets the new baseline for Plans 04+ Power tab polish

---
*Phase: 03.2-broadcast-polish*
*Completed: 2026-04-01*
