---
phase: 03.2-broadcast-polish
plan: 06
type: execute
wave: 3
depends_on:
  - 03.2-03
  - 03.2-04
  - 03.2-05
files_modified:
  - index.html
autonomous: false
requirements:
  - POLI-01
  - POLI-02
  - POLI-03
  - POLI-04
  - POLI-05

must_haves:
  truths:
    - "All 18 tabs have consistent spacing rhythm — no tab looks out of place vs others"
    - "Every interactive element (clickable cards, buttons, sortable headers) shows a hover state"
    - "Tab switch animations are smooth and feel intentional"
    - "Hero section creates a 'wow' impression in the first 3 seconds"
    - "Mobile at 375px feels designed — no horizontal scroll on body, readable text, touch targets"
    - "Loading and empty states feel broadcast-quality across all tabs"
  artifacts:
    - path: "index.html"
      provides: "Final polished state of all 18 tabs"
      contains: "03.2-broadcast-polish"
  key_links:
    - from: "all render functions"
      to: "CSS from Plans 01-02"
      via: "CSS class propagation"
      pattern: "fadeTabIn"
    - from: "index.html <style> block"
      to: "all tab panels"
      via: "global CSS cascade"
      pattern: "PHASE 03.2"
---

<objective>
Cross-tab consistency audit + visual verification checkpoint. After Plans 01-05 have applied CSS and render function changes, this plan does a final pass to catch any inconsistencies, then presents for visual verification.

Purpose: Polish work lives in the details. This plan fixes any residual inconsistencies found during audit and then gates on human visual verification — the only true test of broadcast quality.

Output: Final consistency fixes (spacing gaps, any missed hover states, any broken layouts introduced), then a human visual verification across all 18 tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-broadcast-polish/03.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-tab audit and residual fixes</name>
  <files>index.html</files>
  <read_first>
    Read all CSS sections added by Plans 01-05 — search for all instances of "PHASE 03.2" in index.html to see what was added. Specifically check:
    1. That `.panel.active{display:block}` still exists (not overwritten by fade animation)
    2. That all `.bh` margin rules don't conflict with the existing `.panel > .bh` rules
    3. That the `fadeTabIn` animation doesn't conflict with tabs that use `wipe-in` class
    4. Search for `cursor:pointer` — verify collapsible sections in Trades have it
    5. Search for `🦍` or any emoji — should be 0 results (per no-emoji rule)
    6. Verify mobile nav truncation (D-32) — search for `.nav-btn` text for "MATCHU..." and check if the label is shortened or if the nav scrolls properly. The nav already uses `overflow-x:auto` — this may be acceptable as-is.
    7. Check the D-06 note (Matchups scores are already correct template) — verify no changes degraded them.
    Read the summary of any issues found and decide which need code fixes vs which are acceptable.
  </read_first>
  <action>
    **Residual fix 1: CSS conflict resolution**
    After reading, if `.panel.active{display:block}` has been overwritten or removed:
    ```css
    /* Restore panel display */
    .panel.active{display:block;animation:fadeTabIn 0.2s cubic-bezier(.16,1,.3,1) both}
    ```

    **Residual fix 2: Nav mobile label truncation (D-32)**
    The nav "MATCHU..." truncation happens because `white-space:nowrap` cuts text. Check the nav button text in the HTML — if "MATCHUPS" is truncated, reduce font-size on mobile:
    ```css
    /* D-32: Mobile nav text scaling */
    @media(max-width:480px){
      .nav-btn{padding:12px 14px;font-size:12px;letter-spacing:1px}
      .nav-group-btn{padding:12px 12px;font-size:12px;letter-spacing:1px}
    }
    ```
    This preserves all text but scales down to fit mobile width.

    **Residual fix 3: Verify all decisions are covered**
    Run a quick D-01 through D-57 self-check:
    - D-01: Spacing rhythm CSS ✓ (Plan 01)
    - D-02: .bh margin ✓ (Plan 01)
    - D-03: Card padding ✓ (Plan 01)
    - D-04: Dominant visual element — this is an audit note, not a single fix. The CSS changes collectively move the needle. Note any tabs that still lack a dominant element.
    - D-05: Power circle 64px ✓ (Plan 03)
    - D-06: Matchups scores already correct — verify untouched
    - D-07: Contracts stat cards already good — verify untouched
    - D-08: GM record size ✓ (Plan 03)
    - D-09: Tab fade-in ✓ (Plan 02)
    - D-10: Roster slide-down already exists ✓ (verified in Plan 02)
    - D-11: CountUp extension class ✓ (Plan 02 CSS)
    - D-12: Power circle pulse ✓ (Plans 02+04)
    - D-13: Collapsible hover ✓ (Plans 02+04)
    - D-14: Player name hover ✓ (Plan 02)
    - D-15: Cast strip lift ✓ (Plan 02)
    - D-16: Nav active glow ✓ (Plan 02)
    - D-17: Keeper sort hover ✓ (Plan 02)
    - D-18: Card variants ✓ (Plan 01)
    - D-19: Scroll fades ✓ (Plan 01)
    - D-20: BH consistency ✓ (Plan 01)
    - D-21: Hero LORE card wider ✓ (Plan 03)
    - D-22: Countdown UNTIL ✓ (Plan 03)
    - D-23: Stat strip elevation ✓ (Plan 03)
    - D-24: Cast strip separators ✓ (Plan 03)
    - D-25: Loading screen ✓ (Plan 02 CSS)
    - D-26: Tab skeleton upgrade ✓ (Plan 02 CSS)
    - D-27: Draft empty state ✓ (Plan 03)
    - D-28: Hero text mobile ✓ (Plan 03)
    - D-29: Cast mobile scroll indication ✓ (Plan 03)
    - D-30: Stat strip mobile scroll ✓ (Plan 03)
    - D-31: Contracts filter scroll ✓ (Plan 03)
    - D-32: Nav mobile truncation — fix in this task
    - D-33: Keeper mobile scroll ✓ (Plan 03)
    - D-34: Power tier chyrons ✓ (Plan 04)
    - D-35: Power offseason PF — check renderPower() for where PF displays; if it shows "0 PF" in offseason, update to use `D.lastSeason` data. Add: `const dispPF = D.isOffseason && D.lastSeason ? (lastSeasonTeamData.pf || 0).toFixed(1) : (t.pf||0).toFixed(1)`. This requires reading the actual function — do this fix if the value is clearly wrong.
    - D-36: Roster badge opacity ✓ (Plan 04)
    - D-37: Roster thumbnail surnames ✓ (Plan 04)
    - D-38: Week selector spacing ✓ (Plan 04)
    - D-39: Matchups GOTW card — already broadcast quality, verify untouched
    - D-40: Draft hit rate color ✓ (Plan 04)
    - D-41: Trade grade circles ✓ (Plan 04 CSS)
    - D-42: Trade chevron ✓ (Plan 04)
    - D-43: Scoring legend position ✓ (Plan 04)
    - D-44: Age Map CSS ✓ (Plan 05)
    - D-45: Awards HoF card ✓ (Plan 05)
    - D-46: Analytics H2H breathing ✓ (Plan 05)
    - D-47: Analytics section headers ✓ (Plan 05)
    - D-48: GM career strip ✓ (Plan 05)
    - D-49: GM badge label 11px ✓ (Plan 05)
    - D-50: Rivals CSS ✓ (Plan 05)
    - D-51: Keeper header weight ✓ (Plan 05)
    - D-52: Trophies PRESS PLAY ✓ (Plan 05)
    - D-53: War Room urgency ✓ (Plans 02+05)
    - D-54: Constitution — already excellent, no changes needed ✓
    - D-55: Pulse gauge label ✓ (Plan 05)
    - D-56: Championship story cards elevation ✓ (Plan 05)
    - D-57: Chronicle year redundancy ✓ (Plan 05)

    For any D-XX that was marked ✓ but you're uncertain, add a targeted grep check. If any are missing, add the fix now.

    **Add a phase completion comment to the CSS:**
    At the very end of the Phase 03.2 CSS additions (before `</style>`), add:
    ```css
    /* ═══ PHASE 03.2 BROADCAST POLISH — COMPLETE ═══ */
    /* All 57 design decisions (D-01 through D-57) applied */
    /* Plans: 03.2-01 through 03.2-06 */
    ```
  </action>
  <verify>
    <automated>grep -c "PHASE 03.2" index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PHASE 03.2" index.html` returns at least 6 (one per plan's CSS section)
    - `grep "panel.active{display:block" index.html` returns 1 instance (panel is still visible)
    - `grep "panel.active.*animation:fadeTabIn\|fadeTabIn.*panel.active" index.html` returns 1 instance
    - `grep "🦍\|🏈\|🏆" index.html` returns 0 (no emoji in app UI per CLAUDE.md rule)
    - `grep "BROADCAST POLISH — COMPLETE" index.html` returns the completion comment
    - `grep "nav-btn.*font-size:12px" index.html` returns the mobile nav size fix
  </acceptance_criteria>
  <done>All 57 decisions verified; residual gaps fixed; CSS conflict audit complete; mobile nav size fixed; completion comment added</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Broadcast Polish pass across all 18 tabs:
    - Spacing rhythm: consistent 24px section gaps, normalized broadcast headers
    - Tab transitions: smooth 0.2s fade-in on every tab switch
    - Hover states: every clickable element has visible feedback
    - Hero: wider LORE card, "UNTIL NFL KICKOFF" countdown, elevated stat strip, cast separators
    - Power tab: broadcast chyron tier labels, 64px pulsing score circles
    - Rosters: subtle badge pills, surnames under thumbnails
    - Trades: chevron expand indicators on collapsed sections
    - Draft: color-coded hit rates, broadcast empty state
    - Scoring: legend above heat map
    - Analytics: section broadcast headers, H2H breathing room
    - GM Dashboard: prominent career strip, 11px badge label
    - Trophies: "PRESS PLAY" canvas prompt
    - War Room: urgency color coding (red/yellow/default)
    - Chronicle: de-duplicated year navigation
    - Mobile: horizontal scroll on stat strip, contracts filters, keeper table; better nav sizing
  </what-built>
  <how-to-verify>
    Open index.html in a browser (or at https://amwoods44.github.io/harambes-dozen/ after deploy).

    **Desktop verification (1440px):**
    1. Load the page — confirm hero animation, cast strip avatars stagger in, stat strip shows
    2. Click "HARAMBE'S DOZEN" title area — is the LEAGUE LORE card wider and more commanding?
    3. Check the countdown clock — does it say "UNTIL" before "NFL KICKOFF"?
    4. Click between 3 tabs quickly — do panels fade in smoothly (not pop)?
    5. Power tab: are tier labels broadcast chyrons with angled colored backgrounds? Is the #1 score circle bigger?
    6. Hover over a player name — does it turn red?
    7. Trades tab: hover over collapsed "Trade Record" section — does it show hover state + a chevron?
    8. Scoring tab: is the LOW/MID/GOOD/ELITE legend above the heat map?
    9. Analytics tab: do sections (H2H, All-Play, Consistency) have broadcast headers between them?
    10. War Room tab: if any deadlines are <30 days, does the timer show red?
    11. Trophies tab: does the Standings Race canvas show "PRESS PLAY" prompt?
    12. GM Dashboard: is the W-L record the most visually prominent stat?

    **Mobile verification (375px DevTools):**
    13. Hero — does "15 KEEPERS * PICK TRADING * BONUS SCORING" wrap cleanly?
    14. Stat strip — can you horizontally scroll to see all 5 stats?
    15. Contracts tab — do team filter pills scroll horizontally instead of wrapping 4 rows?
    16. Keeper sheet — does the table scroll horizontally?
    17. Nav — can you read all tab names without extreme truncation?
  </how-to-verify>
  <resume-signal>Type "approved" if the broadcast quality bar is met. Describe specific issues if not (include tab name + what you saw vs what was expected).</resume-signal>
</task>

</tasks>

<verification>
Full visual verification by user covers all 18 tabs. Automated checks:
- `grep -c "PHASE 03.2" index.html` >= 6
- `grep "BROADCAST POLISH — COMPLETE" index.html`
- `grep "panel.active{display:block" index.html`
- No emoji in app UI
</verification>

<success_criteria>
A leaguemate opens the app and says "one person built this? It's better than any fantasy app I've ever seen." Every screen looks like ESPN dynasty coverage. All 57 design decisions from the visual audit are implemented. Mobile at 375px feels designed, not shrunk.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-broadcast-polish/03.2-06-SUMMARY.md`
</output>
