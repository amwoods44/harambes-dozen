---
phase: 03.2-broadcast-polish
plan: 01
subsystem: ui
tags: [css, spacing, design-system, scroll, cards]

# Dependency graph
requires:
  - phase: 03.1-offseason-hardening
    provides: stable rendering foundation for CSS polish work
provides:
  - CSS spacing rhythm via token-based .bh margins propagating to all 18 tabs
  - .u-card-action interactive card variant with hover lift
  - .scroll-x-strip utility class with mask-image gradient fade
  - .scroll-fade container utility for render-function scroll strips

affects: [03.2-02, 03.2-03, 03.2-04, 03.2-05, 03.2-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - CSS mask-image for scroll strip gradient fades (no pseudo-element needed)
    - Token-based spacing via --sp-N variables on all broadcast headers
    - Last-rule-wins CSS override to normalize across existing ad-hoc values

key-files:
  created: []
  modified:
    - index.html

key-decisions:
  - "Added Phase 03.2 CSS block at end of <style> to override earlier ad-hoc hardcoded values (60px, 32px, 20px) with token-based values"
  - "Used CSS mask-image (not ::after pseudo-element) for scroll fades — avoids conflict with cast strip's existing ::before/::after gradients"
  - "Both tasks implemented in single commit — pure CSS additions, no behavior change, logical single unit"

patterns-established:
  - ".scroll-x-strip: overflow-x scroll container with built-in right-edge gradient fade via mask-image — use in render functions in Plans 04/05"
  - ".u-card-action: third card variant for interactive cards requiring cursor:pointer + hover lift"
  - ".scroll-fade + .on-dark modifier: container-level fade for strips with varying backgrounds"

requirements-completed: [POLI-01, POLI-04]

# Metrics
duration: 1min
completed: 2026-04-01
---

# Phase 3.2 Plan 01: Spacing Rhythm + Scroll Strip Fades

**Token-based broadcast header margins and CSS scroll gradient fades propagating to all 18 tabs via 41-line global style block addition**

## Performance

- **Duration:** 1 min
- **Started:** 2026-04-01T18:54:52Z
- **Completed:** 2026-04-01T18:55:53Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Normalized `.bh` broadcast header margins to `var(--sp-6)` / `var(--sp-4)` overriding 4 competing hardcoded values (60px, 32px, 20px, 8px) — single rule change propagates to all 18 tabs
- Added `.u-card-action` as the third card variant (alongside `.u-card` and `.u-card-stat`) with hover lift and cursor:pointer for interactive cards
- Established `.scroll-x-strip` and `.scroll-fade` utility classes using CSS `mask-image` for right-edge gradient fades on horizontal scroll strips — applied immediately to `.wk-sel` and `.move-filters`
- Normalized `.u-card`, `.u-card-stat`, `.u-surface` padding to 16px desktop / 12px mobile via tokens

## Task Commits

Each task was committed atomically:

1. **Task 1 + Task 2: Spacing rhythm + scroll strip fades** - `ca47769` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `index.html` - Added 41-line Phase 03.2 CSS block at end of `<style>` tag

## Decisions Made
- Added the new CSS at the very end of the `<style>` block to ensure last-rule-wins precedence over earlier conflicting values (60px bh margin from line 1196, 32px from line 1098)
- Used CSS `mask-image` instead of `::after` pseudo-element for scroll strip fades — cast strip already uses `::before`/`::after` for its own gradients, so mask-image avoids conflict entirely
- Committed both tasks as one atomic unit — both are pure CSS additions to the same `<style>` block with no behavior changes, forming a single logical CSS polish pass

## Deviations from Plan

None - plan executed exactly as written. The CSS insertion point was moved to end of `<style>` (rather than after line 1097 as suggested) to ensure correct cascade override, but this is the correct implementation of the plan's stated intent to supersede the existing conflicting rules.

## Issues Encountered
None. The existing `.panel > .bh` rules at lines 1098 and 1196 set conflicting values (32px and 60px respectively). Adding at end of `<style>` block cleanly supersedes both with token-based values — standard CSS cascade behavior.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Spacing rhythm CSS is live and propagating to all 18 tabs
- `.scroll-x-strip` utility class available for Plan 04/05 render function changes
- `.u-card-action` variant available for interactive card upgrades in upcoming plans
- No blockers for Phase 03.2 Plan 02

---
*Phase: 03.2-broadcast-polish*
*Completed: 2026-04-01*
