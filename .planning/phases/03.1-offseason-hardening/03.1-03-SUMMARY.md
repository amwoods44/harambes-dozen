---
phase: 03.1-offseason-hardening
plan: 03
subsystem: ui
tags: [offseason, fallback, vanilla-js, render-guards, historical-data]

# Dependency graph
requires:
  - phase: 03.1-offseason-hardening plan 01
    provides: D.isOffseason, D.lastSeason, LAST_COMPLETED_SEASON, computeRecords with week capture
  - phase: 03.1-offseason-hardening plan 02
    provides: scoringStandings, analyticsMatchupWeeks, matchupsData local vars; dirty tabs list updated
provides:
  - awardsStandings/awardsWeeklyScores fallback in renderAwards using last-season data
  - gmStandings/gmWeeklyScores fallback in renderGM; franchiseM career stats strip in each GM card
  - defaultDraft year selection skips years with no picks; selects last year with actual picks
  - renderMoves empty state message changed to 'Loading transaction history...'
  - renderRivals empty state context-aware: 'Loading historical rivalry data...' in offseason
  - renderChronicle: pts>0 and diff>0 guards suppress zero-value record events; null-safe week in date strings
affects: [03.1-offseason-hardening, verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "awardsStandings/gmStandings local var pattern: compute fallback data source once before render loop, same pattern as scoringStandings and analyticsMatchupWeeks from plan 02"
    - "franchiseM lookup pattern: find from D.franchise array with roster_id key, default to zero struct if not found"
    - "Zero-value guard pattern: if(D.records&&D.records.X&&D.records.X.value>0) before pushing chronicle events"

key-files:
  created: []
  modified:
    - index.html

key-decisions:
  - "awardsStandings/awardsWeeklyScores local vars follow exact same pattern as scoringStandings from plan 02 — consistent offseason fallback pattern across all data-consuming render functions"
  - "careerLine added after mgr-stats closing div, before mgr-badges — career totals display without restructuring the mgr-stat grid"
  - "Chronicle week display: null-safe conditional appended to date string — 'Season' alone is valid, 'Wk null' is not"
  - "defaultDraft uses Array.find on draftYears (already sorted descending) — first year with picks.length>0 is the most recent completed draft"

patterns-established:
  - "Offseason local var pattern: var xStandings=CUR_STANDINGS; var xScores=D.weekly_scores; if(D.isOffseason&&D.lastSeason){ xStandings=D['standings_'+D.lastSeason]||CUR_STANDINGS; xScores=D['weekly_scores_'+D.lastSeason]||D.weekly_scores; } — apply this to any future render function using CUR_STANDINGS"
  - "Chronicle zero-guard pattern: if(D.records&&D.records.X&&D.records.X.metric>0) — prevents zero-data events from appearing before historical data loads"

requirements-completed: [OFF-03, OFF-06]

# Metrics
duration: 12min
completed: 2026-03-31
---

# Phase 03.1 Plan 03: Content Tab Offseason Guards Summary

**Offseason fallbacks for Draft, Moves, Rivals, Awards, GM, and Chronicle: last-season data substituted, zero-value events suppressed, career stats added to GM cards**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-03-31T23:00:00Z
- **Completed:** 2026-03-31T23:12:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- renderDraft defaults to last year with actual picks (skips upcoming empty draft year) using Array.find on descending sorted draftYears
- renderAwards and renderGM both use awardsStandings/gmStandings local vars that fall back to D['standings_'+D.lastSeason] during offseason — 2025 awards show instead of zero-value superlatives
- renderGM adds franchiseM career stat strip (total W-L, career PF, title count) below the mgr-stats grid for every manager card
- renderChronicle: pts>0 and diff>0 guards prevent "All-Time High: 0 pts" and "Biggest Blowout: 0 pts" chronicle events; week is now null-safe in date strings
- renderMoves and renderRivals empty states updated to informative loading messages instead of confusing "No data" messages

## Task Commits

1. **Task 1: Draft/Moves/Rivals offseason guards** - `16b9c6e` (fix)
2. **Task 2: Awards/GM/Chronicle offseason guards** - `07191c5` (fix)

## Files Created/Modified

- `/Users/aaronwoods/harambes-dozen-repo/index.html` - renderDraft, renderMoves, renderRivals, renderAwards, renderGM, renderChronicle updated

## Decisions Made

- awardsStandings/awardsWeeklyScores and gmStandings/gmWeeklyScores follow the exact same local-var pattern established by scoringStandings in plan 02 — consistent pattern across all CUR_STANDINGS consumers
- careerLine added after mgr-stats grid div, before mgr-badges — avoids restructuring the 8-cell mgr-stat grid layout
- Chronicle week display uses conditional append: `'Season'+(week?' • Wk '+week:'')` — Season alone is a valid date string when week is null
- defaultDraft uses draftYears.find() — finds most recent year with picks.length > 0; draftYears is already sorted descending so first match is the correct answer

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Phase 03.1 (offseason-hardening) is complete. All 12 D.isOffseason occurrences in place. All five local offseason var patterns (scoringStandings, analyticsMatchupWeeks, matchupsData, awardsStandings, gmStandings) established and consistent. App now handles the zero-game offseason gracefully across all 18 tabs:
- No NaN/Infinity (plan 01, plan 02)
- No empty "No data" states that confuse first-time visitors (plan 03)
- All tabs show meaningful historical data when current season hasn't started

---
*Phase: 03.1-offseason-hardening*
*Completed: 2026-03-31*
